import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFFC9956B);
  static const primaryLight = Color(0xFFF5EDE6);

  // Background
  static const background = Color(0xFFFAF8F5);
  static const surface = Color(0xFFFFFFFF);

  // Text
  static const text = Color(0xFF2C2420);
  static const textMuted = Color(0xFFA09080);
  static const textHint = Color(0xFFC8BFB8);

  // Border
  static const border = Color(0xFFE8E4DF);

  // Semantic
  static const success = Color(0xFF10B981);
  static const error = Color(0xFFEF4444);
}